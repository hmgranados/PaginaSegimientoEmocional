import { FormLogin } from "../../components";

const Login = () => {
    return (
        <div className="login-container">
            <FormLogin />
        </div>
    );
}

export default Login;